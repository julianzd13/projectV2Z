<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projectV2Z">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name="com.example.projectV2Z.Room.NewResRoom"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />


        <activity android:name="com.example.projectV2Z.NewReservaStep2" />

        <activity android:name="com.example.projectV2Z.VistaHoraDispActivity" />

        <activity
            android:name="com.example.projectV2Z.NewReservaStep1"
            android:label="@string/titul_nuevares" />

        <activity
            android:name="com.example.projectV2Z.ReservaDetalleActivity"
            android:label="@string/title_activity_reserva_detalle"
            android:parentActivityName="com.example.projectV2Z.MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name="com.example.projectV2Z.MainActivity"
            android:label="@string/name_misreserv">


        </activity>

        <activity android:name="com.example.projectV2Z.RegistroActivity" />

        <activity android:name="com.example.projectV2Z.LoginActivity" />

        <activity android:name="com.example.projectV2Z.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>